let my_arr = [1,2,3,4]

console.log(my_arr)

let my_arr1 = [1,2,3,4, null, true, undefined]
console.log(my_arr1)

console.log(my_arr1[0])

console.log(my_arr1.length)

// new item  can be added into the arra
// exisitng value can be updated

my_arr1[0] = 10

my_arr1[8]="hello"

console.log(my_arr1)

// arrays are mutable and can be changed, new items can be added, any item can be updated
// arrays are objects only

console.log(typeof my_arr1)
